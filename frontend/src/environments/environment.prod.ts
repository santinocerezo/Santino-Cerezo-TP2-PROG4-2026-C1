// Entorno de PRODUCCIÓN (build para Render).
// ⚠️ Reemplazá la URL por la del backend desplegado en Render.
export const environment = {
  production: true,
  apiUrl: 'https://TU-BACKEND.onrender.com',
};
